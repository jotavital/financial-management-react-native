import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    contentContainer: {
        padding: 20,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
    },
});

export default styles;
