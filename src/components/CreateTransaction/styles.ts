import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonContainer: {
        display: 'flex',
        flexDirection: 'row',
        gap: 8,
        justifyContent: 'center',
    },
    container: {
        display: 'flex',
        gap: 8,
        height: '100%',
        padding: 20,
    },
    loadingContainer: {
        padding: 20,
    },
});
