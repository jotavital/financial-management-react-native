import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        height: '100%',
        padding: 20,
        display: 'flex',
        gap: 8,
    },
    buttonContainer: {
        gap: 8,
        display: 'flex',
        justifyContent: 'center',
        flexDirection: 'row',
    },
    loadingContainer: {
        padding: 20,
    },
});
