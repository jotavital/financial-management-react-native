import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        alignItems: 'center',
    },
    inputContainer: {
        display: 'flex',
        width: '85%',
    },
    logo: {
        width: 350,
        height: 350,
    },
});

export default styles;
